# TradingView to Telegram Bot

Автоматический бот для получения торговых сигналов от индикаторов Bullmart в TradingView и отправки уведомлений в Telegram канал.

## Возможности

- ✅ Получение webhook сигналов от TradingView
- ✅ Автоматическая отправка уведомлений в Telegram канал
- ✅ Поддержка LONG и SHORT позиций
- ✅ Отслеживание состояния позиций
- ✅ Расчёт Take Profit и Stop Loss на основе VWAP и ATR
- ✅ Расчёт Risk/Reward соотношения
- ✅ Форматированные уведомления с эмодзи
- ✅ Мониторинг всех фьючерсных пар Bitget
- ✅ Логирование всех событий
- ✅ Health check endpoints

## Архитектура

```
TradingView (Bullmart Indicators)
         ↓
    Alert System
         ↓
   Webhook (HTTPS)
         ↓
  FastAPI Server
         ↓
 Signal Processor
         ↓
  Telegram Bot API
         ↓
  Telegram Channel
```

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd tradingview-telegram-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
nano .env
```

Заполните следующие переменные:
- `TELEGRAM_BOT_TOKEN` - токен от @BotFather
- `TELEGRAM_CHANNEL_ID` - ID вашего канала
- `WEBHOOK_SECRET` - секретный ключ для аутентификации

### 4. Запуск бота

```bash
python bot.py
```

Бот будет доступен на `http://localhost:8000`

## Развертывание

### Railway (Рекомендуется)

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Добавьте переменные окружения
4. Railway автоматически развернёт бот

### Docker

```bash
docker-compose up -d
```

### Render

1. Создайте Web Service на [render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Настройте переменные окружения
4. Render автоматически развернёт бот

## Настройка TradingView

Подробная инструкция по настройке алертов в TradingView находится в файле [SETUP_GUIDE.md](SETUP_GUIDE.md).

### Краткая версия:

1. Создайте алерт на графике с индикаторами Bullmart
2. Включите Webhook URL: `https://your-app.railway.app/webhook`
3. Добавьте JSON payload в поле Message
4. Добавьте заголовок Authorization: `Bearer your_webhook_secret`
5. Создайте алерты для всех нужных символов

## API Endpoints

### `GET /`
Health check endpoint

**Response:**
```json
{
  "status": "running",
  "service": "TradingView Telegram Bot",
  "version": "1.0.0"
}
```

### `GET /health`
Detailed health check

**Response:**
```json
{
  "status": "healthy",
  "telegram_bot": "ok",
  "active_positions": 0
}
```

### `POST /webhook`
Receive webhook from TradingView

**Headers:**
```
Authorization: Bearer your_webhook_secret
Content-Type: application/json
```

**Request Body (Entry Signal):**
```json
{
  "signal_type": "entry_long",
  "symbol": "BTCUSDT",
  "timeframe": "7m",
  "price": 45000.50,
  "momentum": -5.3,
  "vwap": 45100.00,
  "atr_high": 46000.00,
  "atr_low": 44000.00,
  "timestamp": "2026-02-02T10:30:00Z"
}
```

**Request Body (Exit Signal):**
```json
{
  "signal_type": "exit_long",
  "symbol": "BTCUSDT",
  "timeframe": "7m",
  "price": 45050.00,
  "momentum": -4.8,
  "tp_price": 45100.00,
  "sl_price": 44000.00,
  "timestamp": "2026-02-02T10:37:00Z"
}
```

### `POST /test`
Send test notification to Telegram

**Response:**
```json
{
  "status": "success",
  "message": "Test notification sent"
}
```

## Формат уведомлений

### Вход в позицию

```
🟢 LONG ВХОД В ПОЗИЦИЮ

Символ: ZROUSDT
Таймфрейм: 7m
Цена: $1.760200

📊 Индикаторы:
• Momentum: -5.23 (ниже -5.1)
• VWAP: $1.762900

⏰ Время: 2026-02-02T10:30:00Z

Начат набор позиции. Ожидайте уведомление с TP/SL...
```

### Параметры позиции (TP/SL)

```
🟢 ПАРАМЕТРЫ ПОЗИЦИИ

Символ: ZROUSDT
Направление: LONG
Статус: Набор позиции завершён

🎯 Take Profit: $1.762900 (VWAP)
🛡 Stop Loss: $1.693300 (ATR)

📊 Анализ:
• Momentum: -4.85 (вышел из зоны)
• Risk/Reward: 1:2.14
• Плечо: 5x

⏰ Время: 2026-02-02T10:37:00Z
```

## Структура проекта

```
tradingview-telegram-bot/
├── bot.py                  # Основной файл приложения
├── requirements.txt        # Python зависимости
├── Dockerfile             # Docker конфигурация
├── docker-compose.yml     # Docker Compose конфигурация
├── .env.example           # Пример файла с переменными окружения
├── README.md              # Этот файл
├── SETUP_GUIDE.md         # Подробное руководство по настройке
├── config/                # Конфигурационные файлы
└── logs/                  # Логи приложения
```

## Логирование

Все события логируются в:
- Консоль (stdout)
- Файл `logs/bot.log`

Уровни логирования:
- `INFO` - обычные события (получение webhook, отправка сообщений)
- `WARNING` - предупреждения (неавторизованные запросы)
- `ERROR` - ошибки (проблемы с Telegram API, ошибки обработки)

## Безопасность

- ✅ Аутентификация webhook через Bearer token
- ✅ Валидация входящих данных
- ✅ Переменные окружения для чувствительных данных
- ✅ HTTPS для webhook endpoint
- ✅ Логирование всех событий

## Требования

- Python 3.11+
- TradingView Premium (для webhook alerts)
- Telegram Bot Token
- Доступ к индикаторам Bullmart

## Зависимости

- `fastapi` - веб-фреймворк
- `uvicorn` - ASGI сервер
- `python-telegram-bot` - Telegram Bot API
- `python-dotenv` - управление переменными окружения
- `pydantic` - валидация данных

## Устранение неполадок

### Бот не отправляет сообщения

1. Проверьте TELEGRAM_BOT_TOKEN
2. Убедитесь, что бот добавлен в канал как администратор
3. Проверьте TELEGRAM_CHANNEL_ID

### Webhook не работает

1. Проверьте, что URL доступен публично
2. Убедитесь в правильности WEBHOOK_SECRET
3. Проверьте формат JSON в TradingView alert
4. Посмотрите логи на наличие ошибок

### Неправильные значения индикаторов

1. Убедитесь, что имена plot в TradingView совпадают с ожидаемыми
2. Проверьте, что индикаторы Bullmart правильно настроены

## Лицензия

MIT License

## Поддержка

Для вопросов и поддержки обратитесь к [SETUP_GUIDE.md](SETUP_GUIDE.md)

## Автор

Создано для автоматизации торговых сигналов на основе индикаторов Bullmart.
