# Руководство по настройке TradingView Telegram Bot

Этот бот получает сигналы от индикаторов Bullmart в TradingView и отправляет уведомления в Telegram канал.

## Часть 1: Подготовка Telegram

### Шаг 1: Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Bullmart Trading Signals")
   - Введите username бота (должен заканчиваться на `bot`, например: `bullmart_signals_bot`)
4. BotFather отправит вам токен в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
5. **Сохраните этот токен** - он понадобится для настройки

### Шаг 2: Настройка канала

1. Создайте новый канал в Telegram (если ещё не создан)
2. Добавьте вашего бота в канал как администратора:
   - Откройте настройки канала
   - Перейдите в "Администраторы"
   - Нажмите "Добавить администратора"
   - Найдите вашего бота и добавьте его
   - Дайте права на публикацию сообщений

3. Получите ID канала:
   - Если канал публичный: используйте `@channel_username`
   - Если канал приватный: используйте числовой ID (например: `-1001234567890`)
   - Для получения ID приватного канала:
     - Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
     - Он покажет ID канала

## Часть 2: Развертывание бота

### Вариант A: Развертывание на Railway (Рекомендуется)

Railway предоставляет бесплатный хостинг с 500 часами в месяц.

1. **Создайте аккаунт на Railway:**
   - Перейдите на [railway.app](https://railway.app)
   - Зарегистрируйтесь через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Подключите ваш GitHub аккаунт
   - Загрузите код бота в GitHub репозиторий
   - Выберите репозиторий

3. **Настройте переменные окружения:**
   - В настройках проекта перейдите в "Variables"
   - Добавьте следующие переменные:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
     TELEGRAM_CHANNEL_ID=@ваш_канал_или_chat_id
     WEBHOOK_SECRET=придумайте_сложный_пароль
     PORT=8000
     ```

4. **Получите URL вебхука:**
   - После развертывания Railway предоставит URL вида: `https://your-app.railway.app`
   - Ваш webhook URL будет: `https://your-app.railway.app/webhook`
   - **Сохраните этот URL** - он понадобится для TradingView

### Вариант B: Развертывание на Render

1. Перейдите на [render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - Environment: Python 3
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`
5. Добавьте переменные окружения (как в Railway)
6. Получите URL: `https://your-service.onrender.com/webhook`

### Вариант C: Локальное развертывание (для тестирования)

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Создайте файл .env
cp .env.example .env

# 3. Отредактируйте .env и добавьте ваши данные
nano .env

# 4. Запустите бота
python bot.py
```

**Важно:** Для локального развертывания нужно использовать ngrok или аналогичный сервис для получения публичного URL:

```bash
# Установите ngrok
# Запустите ngrok
ngrok http 8000

# Используйте предоставленный URL для webhook
```

## Часть 3: Настройка алертов в TradingView

### Шаг 1: Создание алерта для входа в позицию (LONG)

1. Откройте график с индикаторами Bullmart на 7-минутном таймфрейме
2. Нажмите на иконку "Будильник" (Alert) или клавишу `Alt + A`
3. Настройте условия алерта:

**Условие:**
```
Bullmart Discounted Momentum
Crossing Down
Value: -5.1
```

4. В разделе "Notifications" включите "Webhook URL"
5. Введите ваш webhook URL: `https://your-app.railway.app/webhook`
6. В поле "Message" вставьте следующий JSON:

```json
{
  "signal_type": "entry_long",
  "symbol": "{{ticker}}",
  "timeframe": "7m",
  "price": {{close}},
  "momentum": {{plot("Bullmart Discounted Momentum")}},
  "vwap": {{plot("Bullmart VWAP")}},
  "atr_high": {{plot("Bullmart ATR High")}},
  "atr_low": {{plot("Bullmart ATR Low")}},
  "timestamp": "{{time}}"
}
```

7. Настройте дополнительные параметры:
   - **Expiration time:** Не ограничено (или установите нужный срок)
   - **Alert name:** "Bullmart LONG Entry - {{ticker}}"
   - **Webhook Headers:** Добавьте заголовок аутентификации:
     - Key: `Authorization`
     - Value: `Bearer ваш_webhook_secret`

8. Нажмите "Create"

### Шаг 2: Создание алерта для входа в позицию (SHORT)

Повторите Шаг 1, но с условием:

**Условие:**
```
Bullmart Discounted Momentum
Crossing Up
Value: 5.1
```

**Message JSON:**
```json
{
  "signal_type": "entry_short",
  "symbol": "{{ticker}}",
  "timeframe": "7m",
  "price": {{close}},
  "momentum": {{plot("Bullmart Discounted Momentum")}},
  "vwap": {{plot("Bullmart VWAP")}},
  "atr_high": {{plot("Bullmart ATR High")}},
  "atr_low": {{plot("Bullmart ATR Low")}},
  "timestamp": "{{time}}"
}
```

### Шаг 3: Создание алерта для выхода из зоны (TP/SL)

Этот алерт срабатывает когда Momentum выходит из зоны покупки/продажи.

**Условие для LONG:**
```
Bullmart Discounted Momentum
Crossing Up
Value: -5.1
```

**Message JSON:**
```json
{
  "signal_type": "exit_long",
  "symbol": "{{ticker}}",
  "timeframe": "7m",
  "price": {{close}},
  "momentum": {{plot("Bullmart Discounted Momentum")}},
  "tp_price": {{plot("Bullmart VWAP")}},
  "sl_price": {{plot("Bullmart ATR Low")}},
  "timestamp": "{{time}}"
}
```

**Условие для SHORT:**
```
Bullmart Discounted Momentum
Crossing Down
Value: 5.1
```

**Message JSON:**
```json
{
  "signal_type": "exit_short",
  "symbol": "{{ticker}}",
  "timeframe": "7m",
  "price": {{close}},
  "momentum": {{plot("Bullmart Discounted Momentum")}},
  "tp_price": {{plot("Bullmart VWAP")}},
  "sl_price": {{plot("Bullmart ATR High")}},
  "timestamp": "{{time}}"
}
```

### Шаг 4: Настройка для всех символов Bitget

Для мониторинга всех фьючерсов на Bitget:

1. Получите список всех торговых пар:
   - Перейдите на [Bitget Futures](https://www.bitget.com/futures/usdt)
   - Список основных пар: BTCUSDT, ETHUSDT, BNBUSDT, SOLUSDT, XRPUSDT, ADAUSDT, DOGEUSDT, и т.д.

2. **Вариант A: Создать алерты для каждой пары вручную**
   - Откройте график каждой пары
   - Создайте 4 алерта (entry_long, entry_short, exit_long, exit_short)
   - Это займёт время, но обеспечит точность

3. **Вариант B: Использовать мультисимвольный алерт (если доступно в Premium)**
   - Создайте один алерт с условием "Any alert() function call"
   - Используйте скрипт Pine Script для мониторинга нескольких символов

## Часть 4: Тестирование

### Тест 1: Проверка работы бота

1. Откройте браузер и перейдите по адресу:
   ```
   https://your-app.railway.app/health
   ```

2. Вы должны увидеть:
   ```json
   {
     "status": "healthy",
     "telegram_bot": "ok",
     "active_positions": 0
   }
   ```

### Тест 2: Отправка тестового уведомления

Отправьте POST запрос на `/test` endpoint:

```bash
curl -X POST https://your-app.railway.app/test
```

Вы должны получить тестовое сообщение в Telegram канале.

### Тест 3: Симуляция webhook от TradingView

```bash
curl -X POST https://your-app.railway.app/webhook \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ваш_webhook_secret" \
  -d '{
    "signal_type": "entry_long",
    "symbol": "BTCUSDT",
    "timeframe": "7m",
    "price": 45000.50,
    "momentum": -5.3,
    "vwap": 45100.00,
    "atr_high": 46000.00,
    "atr_low": 44000.00,
    "timestamp": "2026-02-02T10:30:00Z"
  }'
```

Проверьте, что уведомление пришло в канал.

## Часть 5: Мониторинг и обслуживание

### Просмотр логов

**На Railway:**
- Перейдите в ваш проект
- Откройте вкладку "Deployments"
- Нажмите на активное развертывание
- Просмотрите логи в реальном времени

**Локально:**
```bash
tail -f logs/bot.log
```

### Проверка статуса

Регулярно проверяйте endpoint `/health` для контроля работоспособности.

### Обновление бота

1. Внесите изменения в код
2. Закоммитьте в GitHub
3. Railway автоматически пересоберёт и развернёт новую версию

## Часть 6: Примеры уведомлений

### Уведомление о входе в LONG

```
🟢 LONG ВХОД В ПОЗИЦИЮ

Символ: ZROUSDT
Таймфрейм: 7m
Цена: $1.760200

📊 Индикаторы:
• Momentum: -5.23 (ниже -5.1)
• VWAP: $1.762900

⏰ Время: 2026-02-02T10:30:00Z

Начат набор позиции. Ожидайте уведомление с TP/SL...
```

### Уведомление с TP/SL

```
🟢 ПАРАМЕТРЫ ПОЗИЦИИ

Символ: ZROUSDT
Направление: LONG
Статус: Набор позиции завершён

🎯 Take Profit: $1.762900 (VWAP)
🛡 Stop Loss: $1.693300 (ATR)

📊 Анализ:
• Momentum: -4.85 (вышел из зоны)
• Risk/Reward: 1:2.14
• Плечо: 5x

⏰ Время: 2026-02-02T10:37:00Z
```

## Устранение неполадок

### Проблема: Бот не отправляет сообщения

**Решение:**
1. Проверьте, что бот добавлен в канал как администратор
2. Убедитесь, что TELEGRAM_CHANNEL_ID правильный
3. Проверьте логи на наличие ошибок

### Проблема: Webhook не срабатывает

**Решение:**
1. Проверьте, что webhook URL доступен публично
2. Убедитесь, что WEBHOOK_SECRET совпадает в алерте и в боте
3. Проверьте формат JSON в алерте TradingView
4. Посмотрите логи Railway/Render на наличие входящих запросов

### Проблема: Неправильные значения индикаторов

**Решение:**
1. Убедитесь, что в TradingView алерте используются правильные имена plot:
   - `{{plot("Bullmart Discounted Momentum")}}`
   - `{{plot("Bullmart VWAP")}}`
   - `{{plot("Bullmart ATR High")}}`
   - `{{plot("Bullmart ATR Low")}}`
2. Имена должны точно совпадать с названиями в индикаторе

## Безопасность

1. **Никогда не публикуйте:**
   - TELEGRAM_BOT_TOKEN
   - WEBHOOK_SECRET
   - TELEGRAM_CHANNEL_ID

2. **Используйте сложный WEBHOOK_SECRET:**
   - Минимум 32 символа
   - Случайная строка
   - Пример генерации: `openssl rand -hex 32`

3. **Ограничьте доступ к каналу:**
   - Сделайте канал приватным
   - Добавляйте только доверенных пользователей

## Дополнительные возможности

### Добавление нескольких каналов

Измените код бота для отправки в несколько каналов:

```python
TELEGRAM_CHANNEL_IDS = [
    "@channel1",
    "@channel2",
    "-1001234567890"
]

async def send_telegram_message(message: str):
    for channel_id in TELEGRAM_CHANNEL_IDS:
        await telegram_bot.send_message(
            chat_id=channel_id,
            text=message,
            parse_mode='HTML'
        )
```

### Добавление фильтров по символам

Если хотите получать уведомления только для определённых токенов:

```python
MONITORED_SYMBOLS = ["BTCUSDT", "ETHUSDT", "ZROUSDT"]

@app.post("/webhook")
async def webhook_handler(request: Request):
    payload = await request.json()
    symbol = payload.get('symbol', '')
    
    if symbol not in MONITORED_SYMBOLS:
        return JSONResponse(
            status_code=200,
            content={"status": "ignored", "reason": "symbol not monitored"}
        )
    
    # Continue processing...
```

## Поддержка

Если возникли вопросы или проблемы:

1. Проверьте логи бота
2. Убедитесь, что все переменные окружения установлены правильно
3. Протестируйте каждый компонент отдельно (Telegram, webhook, TradingView)

## Заключение

После завершения настройки вы будете получать автоматические уведомления в Telegram канал при каждом сигнале от индикаторов Bullmart на всех фьючерсных парах Bitget.

Бот работает 24/7 и не требует постоянного контроля.
