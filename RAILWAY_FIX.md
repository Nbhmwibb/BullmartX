# Исправление ошибки Railway Deployment

## Проблема

Railway показывает ошибку:
```
⚠️ Script start.sh not found
❌ Railpack could not determine how to build the app.
```

## Решение

Я добавил 4 конфигурационных файла, которые указывают Railway, как запустить Python приложение:

### 1. `Procfile`
Указывает команду запуска для веб-сервиса.

### 2. `railway.json`
Конфигурация Railway с настройками сборки и развертывания.

### 3. `nixpacks.toml`
Конфигурация Nixpacks (система сборки Railway) для Python.

### 4. `runtime.txt`
Указывает версию Python (3.11.0).

## Как применить исправление

### Вариант 1: Обновить через Git (рекомендуется)

```bash
# 1. Перейдите в папку проекта
cd tradingview-telegram-bot

# 2. Добавьте новые файлы
git add Procfile railway.json nixpacks.toml runtime.txt

# 3. Закоммитьте изменения
git commit -m "Add Railway configuration files"

# 4. Отправьте на GitHub
git push origin main
```

Railway автоматически обнаружит изменения и пересоберёт проект.

### Вариант 2: Загрузить файлы через GitHub веб-интерфейс

1. Откройте ваш репозиторий на GitHub
2. Нажмите "Add file" → "Upload files"
3. Загрузите эти 4 файла:
   - `Procfile`
   - `railway.json`
   - `nixpacks.toml`
   - `runtime.txt`
4. Нажмите "Commit changes"

Railway автоматически пересоберёт проект.

### Вариант 3: Пересоздать репозиторий

1. Удалите старый репозиторий на GitHub
2. Создайте новый
3. Загрузите все файлы из нового архива (включая новые конфигурационные файлы)
4. Переподключите к Railway

## Проверка

После применения исправления:

1. Откройте Railway → ваш проект
2. Перейдите в "Deployments"
3. Дождитесь завершения сборки (должна быть успешной)
4. Проверьте логи - должны увидеть:
   ```
   Starting TradingView Telegram Bot...
   Telegram bot initialized successfully
   ```

5. Откройте в браузере: `https://your-app.railway.app/health`
   - Должен вернуть: `{"status": "healthy", "telegram_bot": "ok"}`

## Что изменилось

**Было:**
- Railway не знал, как запустить Python приложение
- Искал несуществующий `start.sh`

**Стало:**
- Railway знает, что это Python 3.11 приложение
- Знает команду запуска: `python bot.py`
- Автоматически устанавливает зависимости из `requirements.txt`

## Если всё ещё не работает

1. **Проверьте переменные окружения** в Railway:
   - TELEGRAM_BOT_TOKEN
   - TELEGRAM_CHANNEL_ID
   - WEBHOOK_SECRET
   - PORT=8000

2. **Проверьте логи** на наличие других ошибок

3. **Попробуйте Redeploy**:
   - Railway → Deployments → три точки → "Redeploy"

4. **Проверьте GitHub**:
   - Убедитесь, что все файлы загружены
   - Проверьте, что Railway подключен к правильному репозиторию

## Дополнительная информация

Эти конфигурационные файлы стандартны для развертывания Python приложений на Railway и других PaaS платформах (Heroku, Render).

После успешного развертывания бот будет работать 24/7 и принимать webhook от TradingView! 🚀
